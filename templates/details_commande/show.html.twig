{% extends 'base.html.twig' %}

{% block title %}DetailsCommande{% endblock %}

{% block body %}
    <h1>DetailsCommande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ details_commande.id }}</td>
            </tr>
            <tr>
                <th>Qtecommandee</th>
                <td>{{ details_commande.qtecommandee }}</td>
            </tr>
            <tr>
                <th>Prix_unitaire</th>
                <td>{{ details_commande.prixUnitaire }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('details_commande_index') }}">back to list</a>

    <a href="{{ path('details_commande_edit', {'id': details_commande.id}) }}">edit</a>

    {{ include('details_commande/_delete_form.html.twig') }}
{% endblock %}
