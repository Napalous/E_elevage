{{ form_start(form) }}
	<div class="col-12 bottommargin-sm">
		{{ form_row(form.date_livraison, {attr : {'placeholder': "Exemple: Traitement"}}) }}
	</div>
	<div class="col-12 bottommargin-sm">
		{{ form_row(form.commande, {attr : {'placeholder': "Exemple: Image"}}) }}
	</div>
    <button class="btn btn-secondary btn-block btn-lg">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
