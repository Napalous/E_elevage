{% extends 'base.html.twig' %}

{% block title %}Lait{% endblock %}

{% block body %}
    <h1>Lait</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lait.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ lait.quantite }}</td>
            </tr>
            <tr>
                <th>Date_production</th>
                <td>{{ lait.dateProduction ? lait.dateProduction|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('lait_index') }}">back to list</a>

    <a href="{{ path('lait_edit', {'id': lait.id}) }}">edit</a>

    {{ include('lait/_delete_form.html.twig') }}
{% endblock %}
