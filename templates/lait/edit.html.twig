{% extends 'base.html.twig' %}

{% block title %}Edit Lait{% endblock %}

{% block body %}
    <h1>Edit Lait</h1>

    {{ include('lait/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('lait_index') }}">back to list</a>

    {{ include('lait/_delete_form.html.twig') }}
{% endblock %}
