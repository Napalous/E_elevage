{% extends 'base.html.twig' %}

{% block title %}Edit Production{% endblock %}

{% block body %}
	<section id="content">

		<div class="content-wrap bg-light">

			<div class="container">

				<div class="row justify-content-center">
					<div class="col-lg-7 col-md-10">
						<div class="card shadow-sm">
							<div class="card-header">
									<h4 class="mb-0">Enregistrer le nombre de mort</h4>
								</div>	

    {{ include('production/_form.html.twig', {'button_label': 'Enregistrer'}) }}
    </div>
						</div>
					</div>

				</div>

			</div>

		</section>

    <a href="{{ path('production_index') }}">back to list</a>

    {{ include('production/_delete_form.html.twig') }}
{% endblock %}
