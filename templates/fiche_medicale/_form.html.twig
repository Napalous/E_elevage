{{ form_start(form) }}
	<div class="col-12 bottommargin-sm">
		{{ form_row(form.observation) }}
	</div>
	<div class="col-12 bottommargin-sm">
		{{ form_row(form.date_consultation, {attr : {'placeholder': "Exemple: Image"}}) }}
	</div>
	<div class="col-12 bottommargin-sm">
		{{ form_row(form.bovin, {attr : {'placeholder': "Exemple: 0002"}}) }}
	</div>
    <button class="btn btn-secondary btn-block btn-lg">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
