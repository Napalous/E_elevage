{% extends 'base.html.twig' %}

{% block title %}FicheMedicale{% endblock %}

{% block body %}
    <h1>FicheMedicale</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fiche_medicale.id }}</td>
            </tr>
            <tr>
                <th>Observation</th>
                <td>{{ fiche_medicale.observation }}</td>
            </tr>
            <tr>
                <th>Date_consultation</th>
                <td>{{ fiche_medicale.dateConsultation ? fiche_medicale.dateConsultation|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('fiche_medicale_index') }}">back to list</a>

    <a href="{{ path('fiche_medicale_edit', {'id': fiche_medicale.id}) }}">edit</a>

    {{ include('fiche_medicale/_delete_form.html.twig') }}
{% endblock %}
